@tool
extends EditorPlugin

## Minimal EditorPlugin for the gd-fsrs addon. The FSRS classes are exposed via
## `class_name` and need no autoloads, so enabling/disabling the plugin is a no-op.


func _enter_tree() -> void:
	pass


func _exit_tree() -> void:
	pass
